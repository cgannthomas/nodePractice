const xyz = require('./require');
console.log(xyz);

const { people } = require('./require');

console.log(people)

//operating system funcitons list

const os = require('os');

console.log(os);

console.log(os.platform(), os.homedir())